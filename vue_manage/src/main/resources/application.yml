server:
  port: 5273

spring:
  datasource:
    username: root
    password: czc123
    url: jdbc:mysql:///vue_manager

logging:
  level:
    com.czc: debug

mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-not-delete-value: 0
      logic-delete-value: 1